# Tags Slice 2 — N-way merge write path Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use eng:subagent-driven-development to implement this plan task-by-task.

**Goal:** Make a multi-valued tag dimension (caldav `categories`) reorganizable in `organize` — a membership add/remove per bucket placement — by replacing the single-valued assignment model with an N-way (set) merge, with single-valued dimensions falling out as the N=1 degenerate.

**Architecture:** The document projects each object to a SET of grouped-dimension buckets (not one value). Apply computes a per-object membership set-diff (base → edited); each add/remove routes through the tag interpreter's `Complete` (naive: append/drop the exact tag) to produce the object's new full tag set, which the caldav codec persists as its `CATEGORIES`. Single-valued facet dimensions (status/date/priority) stay on their existing `FacetWriteApplier` completion; the placement-diff engine is unified, the write-completion dispatches by field kind. Every existing single-valued organize lane must stay green (the N=1 conformance pin).

**Tech Stack:** Go; `internal/organize` (document + apply), `internal/cutting_garden_plugins` (`TagInterpreter`, unified field codecs), `plugins/caldav` (codec write side); bats against `cutting-garden-caldav-testserver`.

**Rollback:** No dual period (matches #230/D8). Lands as one merge; rollback is `git revert` of that merge. Safe because the N=1 degenerate is conformance-pinned by the untouched single-valued organize bats before tags ever use the general case.

**References:** design D8 (`docs/plans/2026-08-20-tags-design.md`), RFC 0019 (`Complete`, §5 naive), RFC 0015 (organize dialect), FDR 0023 (organize), FDR 0025 (unified field codecs, `DeriveNodeTypeFacetWrites`).

**Prerequisite reading for the implementer (exact signatures this plan does NOT reproduce — read before Task 2/3):**
- `internal/organize/apply.go` — `planMoves`, `resolveWrites` (296-328), `checkMoveWritable` (388-409), the apply driver that calls them.
- `internal/organize/field_apply.go` — `planFieldEdits` (the sibling three-way merge; its `unionAtomNames`/conflict shape is the template for atom reconciliation).
- `internal/cutting_garden_plugins/tag_interpreter.go` — `TagInterpreter`, `TagMembership`, `TagMembershipOp` (`TagAdd`/`TagRemove`), `naiveTagInterpreter.Complete` (164+, already implemented).
- `internal/cutting_garden_plugins/field_unified.go` + `plugins/caldav/unified.go` — how `unifiedFieldSets()` declares a field writable/groupable; `DeriveNodeTypeFacetWrites` derives `FacetWrite{Mode}` from it (write:none vs one vs many). This is where `categories` is made writable.
- `plugins/caldav/facet_apply.go`, `facet_write.go` — the caldav write-back the framework calls.

---

## Design decisions settled going in

- **D4 (rendering) is NOT in this slice.** Slice 2 keeps the current `## =<tag>` bucket spelling. The no-`=` continuation rendering (D4 → decision A) lands with the tag-kind rendering in Slice 3, together with the dodder-hyphen interpreter. Building the write path against today's spelling avoids re-spelling a just-built parser.
- **D5 (`_` lift) is a no-op here.** naive `Normalize`/`SortKey` are already the identity; there is no lift to implement or avoid.
- **Dup-guard resolution.** `document.assignments()` (document.go:382-421) rejects an object under two buckets. That guard is single-valued-correct and MUST stay for single-valued dimensions, but is exactly what blocks a multi-membership apply. Slice 2 introduces a cardinality-aware membership projection: multi (`FacetWriteMany`) → several buckets per object is legal; single (`one`) → two buckets is still the loud error. See Task 1.
- **Write dispatch.** Tag-field membership edits complete through `TagInterpreter.Complete` → new tag set → caldav `CATEGORIES` write-back. Facet-enum edits keep their `FacetWriteApplier` completion. The placement-diff (Task 2) is shared; the completion branches on field kind.
- **Removal.** Deleting a line under a bucket removes that membership PROVIDED the object still appears elsewhere in the document. An object's LAST line vanishing stays #215's `%:allow-deletion` territory (unbuilt) and is rejected as today.

---

## Task 1: Cardinality-aware membership projection

**Promotion criteria:** `assignments()` (single-valued map) is retained only as the `cardinality == one` path; once every caller uses the new projection, the standalone method can be folded in. Keep it until Task 4 lands.

**Files:**
- Modify: `internal/organize/document.go` (add `memberships()` beside `assignments()`; do NOT delete `assignments()` yet)
- Test: `internal/organize/document_test.go`

**Approach:** Add `func (doc document) memberships(multi bool) (map[string][]string, error)`. It walks the same depth-stack as `assignments()` but collects a SLICE of bucket values per id. When `multi` is false it enforces the existing single-value guard (a second DISTINCT bucket for an id is the "appears twice" bad request); when `multi` is true, several distinct buckets are legal and accumulate, but the SAME bucket twice for one id is still malformed (a duplicated line). Ungrouped objects contribute an empty membership set.

**Step 1 — failing test (multi allows several buckets, rejects same-bucket dup):**

```go
func TestMemberships_MultiValued(t *testing.T) {
	doc := document{Sections: []section{
		{Depth: 1, Term: "categories="},
		{Depth: 2, Term: "=work", Lines: []objectLine{{ID: "t1.ics"}}},
		{Depth: 2, Term: "=urgent", Lines: []objectLine{{ID: "t1.ics"}}},
	}}
	m, err := doc.memberships(true)
	if err != nil {
		t.Fatalf("multi membership must be legal: %v", err)
	}
	got := append([]string(nil), m["t1.ics"]...)
	sort.Strings(got)
	if len(got) != 2 || got[0] != "urgent" || got[1] != "work" {
		t.Errorf("t1 memberships = %v, want [urgent work]", got)
	}

	// same bucket twice for one id is still malformed
	dup := document{Sections: []section{
		{Depth: 1, Term: "categories="},
		{Depth: 2, Term: "=work", Lines: []objectLine{{ID: "t1.ics"}, {ID: "t1.ics"}}},
	}}
	if _, err := dup.memberships(true); err == nil {
		t.Error("same bucket twice for one id must reject")
	}
}

func TestMemberships_SingleValuedRejectsTwoBuckets(t *testing.T) {
	doc := document{Sections: []section{
		{Depth: 1, Term: "status="},
		{Depth: 2, Term: "=A", Lines: []objectLine{{ID: "x.ics"}}},
		{Depth: 2, Term: "=B", Lines: []objectLine{{ID: "x.ics"}}},
	}}
	if _, err := doc.memberships(false); err == nil {
		t.Error("single-valued object under two buckets must reject")
	}
}
```

**Step 2 — run, expect FAIL** (`memberships` undefined): `just debug-test-pkg ./internal/organize TestMemberships`

**Step 3 — implement `memberships()`** mirroring `assignments()`'s depth-stack walk, accumulating `[]string` per id; enforce the two guards above. (Keep `assignments()` untouched.)

**Step 4 — run, expect PASS.**

**Step 5 — commit:** `feat(organize): cardinality-aware membership projection (#231 slice 2)`

---

## Task 2: N-way membership merge (`planMemberships`)

**Promotion criteria:** subsumes `planMoves` for the grouped dimension; `planMoves` is retired when Task 4 routes single-valued through the N=1 path of this merge.

**Files:**
- Create: `internal/organize/membership_apply.go`
- Test: `internal/organize/membership_apply_test.go`

**Approach (refined during execution):**
`planMemberships(edited, base document, live []cgp.Node, anchor string, interp cgp.TagInterpreter, dim string) ([]membershipEdit, error)`, where `membershipEdit{URI string; Node cgp.Node; NewTags []string}` carries the complete replacement tag set to persist. Mirror `planFieldEdits`' base/edited/live-by-id structure (`relativeID`). For each id in base ∩ live: `adds = editedSet \ baseSet`, `removes = baseSet \ editedSet` (sets from `memberships(true)`); fold removes then adds through `interp.Complete(newTags, op, bucket)` starting from the LIVE tag set (`node.Facets[dim]` `.Key`s) → `NewTags`.

- **No hard conflict path in this slice.** Tag add/remove commute and `Complete` is idempotent, so folding the doc's set-diff onto the live set is a sound merge; a bucket a concurrent edit already changed becomes a no-op. If `NewTags` is set-equal to the live set, emit nothing.
- **Last-line-vanish = id ABSENT from the edited doc entirely** (was in base, its line deleted) → reject (`#215`). An object still present but moved out of every bucket (present ungrouped ⇒ id IS in `edited.objectLines()`, empty membership) is a LEGAL remove-all (empty `NewTags`), not this case.
- **Atom reconciliation is DEFERRED to the wiring task (Task 4)** — a multi-membership object's several appearances feeding the existing `planFieldEdits` need dedup/agreement handling there; Task 2 is membership-only.

**Steps:** TDD each rule as its own failing test → minimal impl → pass → commit:
- add-only: base `{work}`, edited `{work,urgent}`, live `{work}` → `NewTags {work,urgent}`.
- remove-with-survivor: base `{work,urgent}`, edited `{work}`, live `{work,urgent}` → `NewTags {work}`.
- no-op: base == edited → no edits.
- idempotent: live already `{work,urgent}` for an add of `urgent` → no edit.
- last-line-vanish (id absent from edited) rejects.
- remove-all-legal (present ungrouped) → one edit, empty `NewTags`, no error.

Commit once green (frequent commits).

---

## Task 3: Make caldav `categories` writable (`write:many` + CATEGORIES set write-back)

**Promotion criteria:** `categories`'s `FacetWrite.Mode` flips `none → many`; the read-only declaration is gone.

**Files:**
- Modify: `plugins/caldav/unified.go` (the `categories` field in `unifiedFieldSets()` — declare it writable so `DeriveNodeTypeFacetWrites` derives `many`)
- Modify: the `categories` codec's write side (append/remove a value in the VTODO `CATEGORIES` property, preserving the others)
- Test: `plugins/caldav/facet_write_test.go` (or the codec's own test)
- After editing under a `pkgs/`-facaded package, run `just codemod-generate-dagnabit`.

**Approach:** read `plugins/caldav/unified.go` for how a groupable field declares writability (compare `status`, which is `write:one`). `categories` is multi-valued so its derived mode must be `many` — confirm `DeriveNodeTypeFacetWrites` maps a multi-valued writable field to `FacetWriteMany` (if not, that helper is the real Task-3 change). The write-back takes the interpreter-completed tag SET and rewrites `CATEGORIES:tag1,tag2,…`.

**Steps:** TDD — a unit test that a `TagAdd`/`TagRemove` completed set serializes to the expected `CATEGORIES` line; declare writable; make the codec write; regenerate facade; commit.

---

## Task 4a: SDK membership-write capability (`BuildMembershipWritePatch`)

**Why a NEW method (not `BuildFacetWritePatch`):** `BuildFacetWritePatch` takes a single `toBucket string` and routes through `ParseUnifiedBucketMove`, which calls `codec.Parse({dim: {toBucket}})` — a SINGLE-element set. Against categories' full-set-replace `Parse` (Task 3) that would DISCARD every other tag (confirmed in the Task 3 review). The `many` write is a FULL-SET replacement carrying the complete `NewTags`, so it needs its own path.

**Files:**
- Modify: `internal/cutting_garden_plugins/facet_apply.go` (add the interface), `facet_derive.go` (add `ParseUnifiedMembershipWrite`).
- Modify: `plugins/caldav/facet_apply.go` (implement the method, mirroring `BuildFacetWritePatch`).
- Test: SDK + caldav unit tests.
- Run `just codemod-generate-dagnabit` (interface under `internal/cutting_garden_plugins/` → regenerate the `pkgs/` facade).

**Approach:**
- `ParseUnifiedMembershipWrite(codecs []Codec, dimension string, newTags []string, current map[string]any) (map[string]any, error)` — mirror `ParseUnifiedBucketMove` (facet_derive.go:145) but call `c.Parse(map[string][]string{dimension: newTags}, current)` (the FULL set). Validate groupable + writable + MultiValued; reject read-only/unknown; no closed-domain check (tags are open).
- `type MembershipWriteApplier interface { Plugin; BuildMembershipWritePatch(ctx, node Node, write FacetWrite, newTags []string) ([]byte, error) }` — the set-write sibling of `FacetWriteApplier`. caldav implements it via `ParseUnifiedMembershipWrite` wrapped in the substrate patch shape, exactly as its `BuildFacetWritePatch` does.
- Unit-test: caldav `BuildMembershipWritePatch(node, write, ["work","urgent"])` → a patch that sets CATEGORIES to that set; empty set clears.

## Task 4b: apply-driver dispatch — route `many` → `planMemberships` → execute

**Promotion criteria:** `checkMoveWritable`'s `FacetWriteMany` "out of scope" branch (apply.go:402-406) is replaced by the real apply; single-valued keeps working unchanged.

**Files:**
- Modify: `internal/organize/apply.go` (`applyDocument`).
- Test: `internal/organize/apply_test.go`.

**Approach:** in `applyDocument`, resolve the grouped dimension's write mode EARLY — BEFORE `planMoves`, which calls `assignments()` and would dup-guard-reject a multi-membership doc. Branch on mode:
- **`many`** (categories): select the interpreter (naive default via RFC 0019 §4 field default; config override is Slice 3), run `planMemberships(edited, base, live, anchor, interp, dim)`, and execute each `membershipEdit` via `MembershipWriteApplier.BuildMembershipWritePatch` + `NodeMutator.PatchNode`. Render a minimal change preview (one line per object naming the tag-set delta) under the same dry-run/confirm/commit gates. Do NOT run `planMoves` for `many`.
- **`one`/facet** (status/date/priority): the existing `planMoves` + `FacetWriteApplier` path, untouched (the N=1 regression pin, Task 6).
- **`none`**: unchanged read-only rejection.
- **`planFieldEdits` on a multi-membership doc:** the object's line appears N times, so `planFieldEdits` sees the id N times. Minimal safe option: DEDUP its output by URI (apply an object's field edit once), or skip field-edit reconciliation for the `many` grouping. Pin it — a two-appearance object with DIVERGENT atoms must NOT silently double-write (assert one edit or a loud conflict). Full agree/conflict reconciliation is Slice 2b.

Update `TestApply_MultiValuedModeNoneRejectsMove` / the `FacetWriteMany` rejection tests: the many case now APPLIES.

---

## Task 5: End-to-end lane + flip the slice-1 rejection

**Files:**
- Modify: `zz-tests_bats/organize_tags.bats`

**Approach:** the slice-1 lane pinned that applying a moved categories document is REJECTED (the `organize_categories_apply_rejected` test asserting "appears twice"). Slice 2 makes it succeed:
- Rewrite that test to MOVE a one-tag task's categories (add a second tag, or move it to a different tag) and assert the live `CATEGORIES` changed (curl the `.ics`, like `organize_date`'s reschedule test).
- Add a multi-membership round-trip case: a two-tag task rendered under both buckets applies cleanly (no "appears twice"), and removing one appearance drops that one tag while the object survives under the other.
- Keep `organize_categories_multi_membership` and `list_facets_categories_filter` green (rendering unchanged in this slice).

---

## Task 6: Single-valued regression pin

**Files:** none new — this is a gate.

**Approach:** confirm `organize.bats` (status), `organize_priority.bats`, `organize_date.bats`, `organize_fields.bats` are all green unchanged — the N=1 degenerate must reproduce the exact prior single-valued behavior. This is the rollback-safety guarantee; if any single-valued lane needs a change, the unification in Task 2/4 is wrong. The full suite runs in the `merge-this-session` pre-merge hook.

---

## Out of scope (Slice 3)

Tag-kind rendering (D4 no-`=` continuation `## -client` + naive bare `## work`, quoting space-bearing tags), the dodder-hyphen interpreter (hierarchy, namespace rollup, transitive `Matches`, rollup-bucket `Complete`), bare-tag trellis terms, the `[tags] interpreter` config override, and `#232` tag-object editing.
